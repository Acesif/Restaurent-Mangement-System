spring:
  application:
    name: RestaurentManagement
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/restaurent
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
