spring:
  application:
    name: RestaurantManagement
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/restaurant?createDatabaseIfNotExist=true&useSSL=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
